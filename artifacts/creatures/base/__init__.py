"""Built-in creature definitions.

Each creature is defined as Python data: stats, collision box, walk speed,
default behavior, model reference. The C++ engine loads these at startup.

Players can fork any creature definition and modify it via the in-game editor.
"""
