"""Living entities — humanoids and animals.

Every living thing uses the same `living = {...}` dict schema.
`category` is either "humanoid" or "animal" (two groups, nothing else).
All living entities are playable by default — pick them at the main menu.
"""
